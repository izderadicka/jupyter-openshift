apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jupyter-chart.fullname" . }}-test-connection"
  labels:
    {{- include "jupyter-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jupyter-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
